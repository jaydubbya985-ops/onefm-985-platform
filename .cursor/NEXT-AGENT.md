# NEXT CLOUD AGENT — paste this as the whole prompt

You are continuing ONE FM 98.5. Benchmark: **80% = gov-ready / send-to-FOOTT without DEMO or invented stats**. Public `/proposal` now requests a staff PDF (no fake 38% / AI toy). Do not wait. Pull latest. Prefer PR #9 branch if unmerged.

## Single next action (do this first)

**One shared PDF letterhead helper.** Invoices (`InvoiceEmailTemplate`), proposals (`proposalDocument.ts`), and contracts (`contractDocument.ts`) copy-paste the same navy/gold header. Extract one `drawLetterhead()` used by all three. Do not change BSB `083-894` (`src/lib/bankDetails.ts`) or sourced 39,375 / 25 towns.

Then `npm run build`, commit, push, update the PR.

## After that, in order (toward 80%)

1. Live ops only if env present; else `NEED JAY: VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY on Netlify`. Do not fake live mode.
2. Invoice email: real send or honest mailto. Never claim sent if it was not.
3. Stripe test on live URL only if `VITE_STRIPE_PUBLISHABLE_KEY` starts with `pk_`. Else NEED JAY one line.
4. Truth grep `src/` for Plemo, unsplash, AI-Enhanced, invented age %. `roiComparison.socialMedia.trust: 38%` in `pricing.ts` is unused — delete or source it, do not put it on public pages.
5. Merge-ready PR #9: ops proposal + contract PDFs + truthful public `/proposal`.

## Rules

- Never fabricate stats, sponsors, or people photos
- DEMO CRM stays labelled DEMO
- Desktop — skip Galaxy Fold work
- Leave the next slice started, not a planning doc
