# NEED JAY — Ops Portal Unblock — 9 Sep 2026

## What actually happened (diagnosis)

The 3.3-billion-token Cursor burn was an **expired `NETLIFY_AUTH_TOKEN`**.
The 2026-07-28 tech run proved it: they reverted the deploy config to the
exact bytes that worked on 2026-07-06 and it *still* failed with
`Unauthorized`. No agent can fix an expired dashboard token — every retry
just burns tokens. The invoice-send code itself is **done and verified**.

## What is already done (Kimi, 9 Sep 2026)

- [x] Full repo copied to workspace — `C:\Users\jaydu\Documents\kimi\workspace\onefm-985-platform`
- [x] Supabase project verified: **ACTIVE_HEALTHY**, all 10 ops tables exist
      with RLS enabled, schema already applied (`ops_invoices` has 2 rows)
- [x] Supabase publishable key retrieved (see Step 2)
- [x] **Deploy fix written & committed** on branch `fix/deploy-netlify-cli`:
      official Netlify CLI (reads `netlify.toml` natively → Functions +
      redirects deploy correctly), Node 20→22. Build verified green.
- [x] Nothing pushed to GitHub yet — waiting on you (Step 1 first).

## YOUR 15 MINUTES — in this exact order

### Step 1 — Regenerate the Netlify token (GitHub) — 5 min

1. https://app.netlify.com → **User settings → Applications → Personal access tokens → New access token** (any name, e.g. `github-deploy`)
2. Copy it immediately
3. https://github.com/jaydubbya985-ops/onefm-985-platform → **Settings → Secrets and variables → Actions**
4. Update **`NETLIFY_AUTH_TOKEN`** with the new token
   (also confirm `NETLIFY_SITE_ID` = `8df4de74-d9a8-42ce-9316-61bd06475c94` — AGENTS.md says it's set ✓)

### Step 2 — Add the build secrets (GitHub) — 3 min

Same GitHub secrets page, add these two **new repository secrets**
(GitHub Actions builds the site — these get baked into the bundle):

```
VITE_SUPABASE_URL      = https://myarjdatdtchmkgdpsab.supabase.co
VITE_SUPABASE_ANON_KEY = sb_publishable_gkdenEdJXMIO_lxXkwzfvw_wfn4vTUq
```

### Step 3 — Add the email key (Netlify) — 5 min

1. https://resend.com → sign up free (100 emails/day — enough for the
   ~104-invoice backlog across two days)
2. **API Keys → Create** → copy `re_...`
3. Netlify → **onefmops** site → **Site settings → Environment variables → Add**
   → `RESEND_API_KEY` = your `re_...` key
   (server-side only, no `VITE_` prefix — never reaches the browser)

Optional later (not blocking): `VITE_STRIPE_PUBLISHABLE_KEY`,
`VITE_GOOGLE_MAPS_API_KEY` — EFT bank details already work without Stripe.

### Step 4 — Tell me, and I push

Once Steps 1–3 are done, say the word. I merge `fix/deploy-netlify-cli` to
`main`, push, and watch the deploy run. Then:

### Step 5 — Verify live (I do this)

- `/.netlify/functions/send-invoice` responds (any non-404 = functions deployed)
- Ops portal at `onefmops.netlify.app/#/ops` — flip from demo password to real login

### Step 6 — Send the backlog (you, ~10 min)

1. Ops portal → Invoices → **Batch Send** → confirm the
   **Email Service banner shows ON**
2. Toggle **Test Mode** → send ONE invoice to your own inbox → check it
3. Test Mode OFF → **Select All Unsent** → send the backlog
4. Money starts coming in

## Deleting Cursor — safe NOW

- Source of truth = **GitHub** (`jaydubbya985-ops/onefm-985-platform`) ✓ untouched
- Workspace copy of the full repo (with `.git` history) ✓ made tonight
- Before closing the account: cancel the subscription at cursor.com/settings
  so you aren't billed again
- Uninstall the app whenever — the code was never inside Cursor

## One thing to watch

With the token fixed, **every push to `main` auto-deploys**. That's the
intended behaviour — just don't push half-finished work to `main`. Feature
branches from now on; I handle that.
