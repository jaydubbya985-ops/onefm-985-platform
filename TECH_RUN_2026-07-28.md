# ONE FM 98.5 — TECH RUN, Tue 28 Jul 2026 (afternoon)

**Mission:** Cashflow is blocked until the ops portal can reliably send real invoices to sponsors. Everything else on the tech-run punch list is secondary to unblocking the invoice send path.

## Scope

- **In scope:** invoice generation, invoice email/PDF send, ops portal auth/data wiring, truth-data audit (no fake stats/photos/names on public pages), Netlify/GitHub Actions deploy pipeline reliability.
- **Out of scope:** Pleming/FWC-related work (not part of this run). Heritage/Drive photo sync, ops IA sidebar redesign, DNS changes — per `CLAUDE-GO-LIVE-SESSION.md` non-negotiables.

## Agents on this run

- **Agent A** — reconnaissance, live-site smoke test, deploy-pipeline audit, truth grep. See `TECH_RUN_STATUS_2026-07-28.md` for full findings.

## Ground rules (carried from AGENTS.md)

1. `git pull origin main` before editing.
2. `npm run build` must pass before every commit.
3. Never fabricate public-facing stats, names, or photos — see `.cursor/rules/gov-truth-data.mdc`.
4. No "Plemo" anywhere — breakfast is "ONE FM Breakfast" with rotating real hosts from `src/data/programGuide.ts`.
5. Credentials/secrets blockers → stop and post `NEED JAY:` with one specific action. Do not invent workarounds.
6. Commit small, push to `main` often — auto-deploy via `.github/workflows/deploy.yml` picks it up.
